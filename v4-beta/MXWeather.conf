server {
    listen 80;

    root /opt/CumulusMX/publicweb;
    index index.htm

    # log files
    access_log /var/log/nginx/MXWeather.access.log;
    error_log /var/log/nginx/MXWeather.error.log;

    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }

    location = /robots.txt {
        allow all;
        log_not_found off;
        access_log off;
    }

    location = /realtime.txt {
        allow all;
        log_not_found off;
        access_log off;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|txt)$ {
        expires max;
        log_not_found off;
    }
}

# Admin interface proxy
server {
    listen 8998;
    
    access_log /var/log/nginx/MXWeather-admin.access.log;
    error_log /var/log/nginx/MXWeather-admin.error.log;
    
    location / {
        proxy_pass http://127.0.0.1:8998;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }
}